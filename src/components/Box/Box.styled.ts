/**
 * Import styled
 */
import { styled } from '@mui/system';

/**
 * Stiled the Container
 */
export const Container = styled('div')(() => {
  return {
    height: '15em',
    width: '15em',
  };
});
